<div class="tab-container">
    <div class="description-container">
        <div class="desc-wrapper">
            <div class="desc-header">{{info.name}}</div>
            <div class="desc-content text">{{product.nameGeo}}</div>
        </div>
        <div class="desc-wrapper">
            <div class="desc-header">{{info.condition}}</div>
            <div class="desc-content text">{{product.condition}}</div>
        </div>
        <div class="desc-wrapper desc-overflow">
            <div class="desc-header">{{info.description}}</div>
            <div class="desc-content text">{{product.description}}</div>
        </div>
    </div>
</div>
